apiVersion: v2
name: kubernetes-mcp-server
description: Local wrapper chart for the Kubernetes MCP server
type: application
version: 0.1.0

dependencies:
  - name: kubernetes-mcp-server
    version: ">=0.0.1"
    repository: oci://ghcr.io/containers/charts