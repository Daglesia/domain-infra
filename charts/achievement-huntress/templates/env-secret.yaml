apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.web-env.name" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.web.secretEnv }}
  {{ $k | upper | replace "-" "_" }}: {{ $v | toString | b64enc | quote }}
  {{- end }}
