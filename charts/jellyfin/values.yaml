jellyfin:
  persistence:
    media:
      size: 15Gi
      existingClaim: local-pvc
  ingress:
    enabled: true
    className: nginx
    hosts:
      - host: media.daglesia.com
        paths:
          - path: /
            pathType: Prefix