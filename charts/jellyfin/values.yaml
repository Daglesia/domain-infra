jellyfin:
  ingress:
    enabled: true
    className: nginx
    hosts:
      - host: media.daglesia.com
        paths:
          - path: /
            pathType: Prefix
  persistence:
    media:
      type: hostPath
      hostPath: /media